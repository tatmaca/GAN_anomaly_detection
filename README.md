# GAN_anomaly_detection

repo for finm 37601 assignment 4

# running `LOB_GAN_training.py`

`python3 LOB_GAN_training.py --stock {stock_name} --lrg {gen learning rate} --lrd {dis learning rate}`

# running `LOB_GAN_testing.py`

`python3 LOB_GAN_training.py --stock {stock_name}`
